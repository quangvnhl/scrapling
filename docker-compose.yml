version: '3.8'

services:
  scraper:
    build: .
    ports:
      - "7860:7860"    
    restart: unless-stopped
    container_name: shopee_scraper_api
    command: uvicorn app:app --host 0.0.0.0 --port 7860 --reload
